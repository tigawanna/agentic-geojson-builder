/**
 * Storage backend selected at scaffold time by create-era-next.
 * Swap this re-export to switch backends later (see docs/04-storage.md).
 */
export { backend } from "@main/storage/backend-store.js";
