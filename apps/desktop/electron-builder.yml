appId: com.tigawanna.agentic-geojson-builder
productName: "Agentic GeoJSON Builder"
copyright: Copyright © 2026 ${author}

directories:
  output: release/${version}
  buildResources: build

files:
  - out/**/*
  - package.json
  - "!node_modules/**/*"
  - from: .packaging-deps/node_modules/@electric-sql
    to: node_modules/@electric-sql
  - from: .packaging-deps/node_modules/sharp
    to: node_modules/sharp
  - from: .packaging-deps/node_modules/detect-libc
    to: node_modules/detect-libc
  - from: .packaging-deps/node_modules/semver
    to: node_modules/semver
  - from: .packaging-deps/node_modules/@img
    to: node_modules/@img

asar: true
asarUnpack:
  - "**/*.node"
  - "**/*.so"
  - "**/*.so.*"
  - node_modules/@electric-sql/**/*
  - node_modules/@img/**/*
  - node_modules/sharp/**/*

extraResources:
  - from: drizzle-pglite/migrations
    to: drizzle-pglite/migrations

# --- Publish -----------------------------------------------------------------
publish:
  provider: github
  owner: "tigawanna"
  repo: "agentic-geojson-builder"
  releaseType: release

# --- macOS -------------------------------------------------------------------
mac:
  target:
    - target: dmg
      arch: [arm64, x64]
    - target: zip
      arch: [arm64, x64]
  category: public.app-category.developer-tools
  icon: build/icon.icns
  hardenedRuntime: true
  gatekeeperAssess: false
  # To enable code signing + notarization, set these env vars before `npm run release`:
  #   APPLE_ID, APPLE_APP_SPECIFIC_PASSWORD, APPLE_TEAM_ID, CSC_LINK, CSC_KEY_PASSWORD
  # See docs/07-build-and-package.md for the full guide.
  entitlements: build/entitlements.mac.plist
  entitlementsInherit: build/entitlements.mac.plist

dmg:
  artifactName: "${productName}-${version}-${arch}.${ext}"

# --- Windows -----------------------------------------------------------------
win:
  target:
    - target: nsis
      arch: [x64, arm64]
    - target: portable
      arch: [x64]
  icon: build/icon.ico
  # Code signing: set WIN_CSC_LINK + WIN_CSC_KEY_PASSWORD before `npm run release`.

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  allowElevation: true
  artifactName: "${productName}-Setup-${version}.${ext}"

# --- Linux -------------------------------------------------------------------
linux:
  target:
    - target: AppImage
      arch: [x64, arm64]
    - target: deb
      arch: [x64]
  category: Development
  icon: build/icon.png
  maintainer: "tigawanna"

appImage:
  artifactName: "${productName}-${version}-${arch}.${ext}"

deb:
  fpm:
    - "build/com.tigawanna.agentic-geojson-builder.metainfo.xml=/usr/share/metainfo/com.tigawanna.agentic-geojson-builder.metainfo.xml"
    - "build/screenshots/=/usr/share/metainfo/screenshots/"
